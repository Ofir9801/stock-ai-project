# Full environment for local development and CI.
# Installs both services so the test suite (which imports the backend) and local
# runs of either service work from a single `pip install -r requirements.txt`.
# The Docker images install only their own service's file to stay lean.
-r requirements-backend.txt
-r requirements-frontend.txt
