[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
# E/F are ruff's defaults; ignore line-length since this is a small project
ignore = ["E501"]
